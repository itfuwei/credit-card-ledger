# 卡账项目计划

## 产品目标

为约 10 张信用卡提供本地优先的交易流水、账单周期、额度占用和还款计划管理。用户每次只需记录交易日期、信用卡和金额，系统自动计算归属账单、预计出账日、还款日、已出账/未出账和剩余额度。

## 技术方案

- 前端：Vue 3、Vite、JavaScript、Composition API
- 桌面容器：第二阶段评估 Tauri，复用现有 Web 前端
- 数据库：SQLite；浏览器原型阶段先使用 localStorage/IndexedDB
- 状态管理：第一版使用 composables；跨页面状态复杂后再引入 Pinia
- 日期规则：独立领域模块，不把计算散落在组件中
- 数据安全：本地存储，不记录完整卡号、CVV、密码或短信验证码

## 核心数据

### Card

`id`、`bank`、`name`、`last4`、`limit`、`statementDay`、`paymentDay`、`status`

### Transaction

`id`、`cardId`、`date`、`amount`、`fee`、`note`、`createdAt`

### Repayment

`id`、`cardId`、`billingCycle`、`date`、`amount`、`note`

### BillingCycle（计算结果）

`cardId`、`statementDate`、`paymentDate`、`issuedAmount`、`repaidAmount`、`remainingAmount`、`status`

## 账单规则

1. 交易日期不晚于当月账单日时，归入当月账单；晚于账单日则归入下月账单。
2. 还款日在账单日之后时，与账单日在同一自然月；还款日在账单日之前时，属于下一个自然月。
3. 29、30、31 日遇到短月份时，使用该月最后一天。第一版录入界面可优先限制为 1–28 日，后续完整支持月末规则。
4. 已出账/未出账以当前日期和账单周期计算，不由用户重复维护。
5. 实际应还金额需要扣除已登记还款，并允许退款、撤销和账单调整记录。

## 迭代计划

### Phase 1：可交互原型

- 总览、卡片状态和流水列表
- 快速记录交易
- 自动计算预计出账日、还款日和额度占用
- 响应式桌面/移动布局

### Phase 2：可用 MVP

- 卡片新增、编辑、停用
- 流水增删改查与筛选
- 还款登记及账单结清状态
- SQLite 数据持久化与数据迁移
- 未来 7/15/30 天还款计划
- CSV/Excel 导入导出和本地备份

### Phase 3：稳定性与体验

- 退款、撤销、分期、手续费和账单调整
- 月末账单日及节假日顺延设置
- 重复录入检测、数据校验与审计日志
- 本地通知和日历提醒
- 自动化测试：账单边界、跨年、闰年、短月份、还款冲销

### Phase 4：可选增强

- Tauri 桌面安装包
- 设备间加密同步
- 统计报表和月度资金成本分析

## 第一版验收标准

- 可以维护至少 20 张卡片。
- 新增交易后，总占用、剩余额度和交易列表即时更新。
- 每笔交易明确显示预计出账日与还款日。
- 账单日跨月、跨年和短月份计算正确。
- 数据关闭应用后仍然存在（MVP 阶段）。
- 不存储完整卡号及任何支付认证信息。

## 下一步开发顺序

1. 将示例数据替换为 localStorage repository，完成刷新后持久化。
2. 完成信用卡管理页和交易编辑/删除。
3. 新增还款实体，按账单周期计算剩余应还。
4. 为 `billing.js` 添加边界单元测试。
5. 确认是否以 Tauri + SQLite 作为正式本地应用形态。
